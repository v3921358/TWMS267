package Net.server.maps.field;

import Net.server.maps.MapleMap;

public class OXQuizField extends MapleMap {
    public OXQuizField(int mapid, int channel, int returnMapId, float monsterRate) {
        super(mapid, channel, returnMapId, monsterRate);
    }
}
